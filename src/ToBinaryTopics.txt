1)   int to string
        String s=String.valueOf(binary);
        
        binary is an integer
        
        
        
 2)     StringBuilder sb=new StringBuilder(s);
		sb.reverse();
		String finals=sb.toString();
		
		
		this is string builder is strings
		
3)   max  method   is the collections   


        int finalcount=(int) Collections.max(al);
		System.out.println(finalcount);

      